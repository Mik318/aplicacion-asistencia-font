import {RouteContext} from "../../../shared/model/routing/route-context";

export class InicioRoutes {
  static readonly inicio: RouteContext = {
    uri: '/',
    ucname: 'Inicio de la aplicacion'
  }
}
